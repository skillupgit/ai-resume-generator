version: '3.9'
services:
  db:
    image: postgres:16
    container_name: airezume_db
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: airezume
    volumes:
      - dbdata:/var/lib/postgresql/data
volumes:
  dbdata:
